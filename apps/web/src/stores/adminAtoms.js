import { atom } from 'jotai';
export const roomsAtom = atom([]);
export const settingsAtom = atom({});
export const logsAtom = atom([]);
export const logsLevelAtom = atom('all');
export const logsLimitAtom = atom(200);
export const blockedUsersAtom = atom([]);
